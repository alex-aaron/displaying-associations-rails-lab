<h1>Songs</h1>
<ul>
    <% @songs.each do |song| %>
        <% artist = Artist.find_by_id(song.artist_id) %>
        <li><a href="<%= song_path(song) %>"><%= artist.name %> - <%= song.title %></a></li>
    <% end %>
</ul>